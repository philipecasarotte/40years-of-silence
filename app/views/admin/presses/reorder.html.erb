<h1>Reorder</h1>

<ul id="order">
	<% @items.each do |press| %>
	<li id="item_<%=press.id%>"><%= press.title %></li>
	<% end %>
</ul>

<%= sortable_element 'order', 
      :complete => visual_effect(:highlight, 'order'), 
      :url => { :action => "order" } %>

<p><%= link_to "Back", admin_presses_path %></p>